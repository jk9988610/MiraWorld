# Deprecated name kept for older scripts: same as nginx-miraworld.conf (IP path, no domain redirect).
location = /miraworld {
    return 301 /miraworld/;
}

location ^~ /miraworld/ {
    root /var/www/html;
    index index.html;
    try_files $uri $uri.html /miraworld/index.html;
}
